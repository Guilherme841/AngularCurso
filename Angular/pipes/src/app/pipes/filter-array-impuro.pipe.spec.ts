import { FilterArrayImpuroPipe } from './filter-array-impuro.pipe';

describe('FilterArrayImpuroPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArrayImpuroPipe();
    expect(pipe).toBeTruthy();
  });
});
